var options = {
	choices: [
		[1, "one"], 
		[2, "two"],  
		[3, "three"]
	],
	allow_empty: true,
	empty_text: "empty_value"
}

var form = new FVForm()
.add_field("choice", new FVChoiceField("Choice field", options));